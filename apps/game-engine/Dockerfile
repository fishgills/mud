# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build @mud/game-engine`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t @mud/game-engine`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system @mud/game-engine && \
          adduser --system -G @mud/game-engine @mud/game-engine

COPY apps/game-engine/dist @mud/game-engine/
RUN chown -R @mud/game-engine:@mud/game-engine .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix @mud/game-engine --omit=dev -f install

CMD [ "node", "@mud/game-engine" ]
