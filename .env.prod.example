# Copy this file to `.env` on the production host and replace placeholder values.
# Docker Compose will load variables from `.env` automatically.

PROJECT_ID=battleforge-444008
REGION=us-central1
ARTIFACT_REPO=mud-registry

POSTGRES_DB=mud
POSTGRES_USER=mud
POSTGRES_PASSWORD=please-change-me

OPENAI_API_KEY=replace-with-openai-key

SLACK_BOT_TOKEN=replace-with-slack-token
SLACK_SIGNING_SECRET=replace-with-slack-signing-secret
SLACK_APP_TOKEN=replace-with-slack-app-token
SLACK_CLIENT_ID=replace-with-slack-client-id
SLACK_CLIENT_SECRET=replace-with-slack-client-secret
SLACK_STATE_SECRET=replace-with-slack-state-secret

# Optional overrides
DM_API_BASE_URL=http://dm:3000
WORLD_API_BASE_URL=http://world:3001/world
TICK_INTERVAL_MS=60000
ACTIVITY_THRESHOLD_MINUTES=30

# Image tag to deploy (set automatically by GitHub Actions)
APP_TAG=latest
