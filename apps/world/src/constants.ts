/**
 * Re-export biomes from @mud/constants for backwards compatibility
 * @mud/constants is now the single source of truth for biome definitions
 */
export { BIOMES_BY_KEY as BIOMES } from '@mud/constants';
